import 'package:flutter/material.dart';

class MessInfoPage extends StatelessWidget {
  const MessInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
